# Code node

The code node is composed of input and output interfaces. Inputs are among basic interface designed as function 
arguments in codes and outputs are function outomes. User can modify values in input interfaces or connect them to other
node outputs.